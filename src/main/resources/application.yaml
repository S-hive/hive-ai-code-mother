spring:
  application:
    name: hive-ai-code-mother
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hive_ai_code_mother
    username: root
    password: Root@321

server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.hive.hiveaicodemother.controller
# knife4j的增强配置
knife4j:
  enable: true
  setting:
    language: zh_cn
